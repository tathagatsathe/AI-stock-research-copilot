services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-stock-analysis-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
